# Generated by Chef
# Local modifications will be overwritten.
#
# Bareos Director Job Configuration File

Job {
  Name = "<%= @job_name %>"
<% @job_config.each do |key, value| %>
  <%= key %> = <%= value %>
<% end %>
<% @job_runscript_config.each do |runscript_comment, runscript_components| %>
  Run Script {     # <%= runscript_comment %>
  <% runscript_components.each do |key, value| %>
    <%= key %> = <%= value %>
  <% end unless runscript_components.nil? %>
  }
<% end unless @job_runscript_config.nil? %>
}
